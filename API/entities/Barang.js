class Barang {
  constructor({ id, nama, gambar, kategori, stok }) {
    this.id = id;
    this.nama = nama;
    this.gambar = gambar;
    this.kategori = kategori;
    this.stok = stok;
  }
}

module.exports = Barang;
